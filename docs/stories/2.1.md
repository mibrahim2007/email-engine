# Story 2.1 — Mailbox model and connection framework

| | |
|---|---|
| **Epic** | 2 — Mailbox connection and ingest |
| **Status** | Draft — **not Approved.** Depends on all of Epic 1 |
| **Drafted** | 2026-08-05, SM |
| **Delivers** | FR10 (partly), FR11 (health half). Foundation for FR6–FR9 |
| **Creates** | `mailboxes`, `conversations`, `messages`, `contacts`, `attachments` |
| **Depends on** | 1.2 (Drizzle), 1.3 (`withTenant`, the `policies/` pattern), 1.5 (`audit()`), 1.7 (`notify()`) |

## Story

**As a developer,**
**I want one mailbox model and a uniform connector interface,**
**so that the rest of the system never branches on provider.**

## Acceptance criteria

1. `mailboxes` table and RLS policy exist per the architecture.
2. A `MailboxConnector` interface defines `connect`, `refresh`, `fetchSince`, `send`, and `revoke`.
3. Credentials are encrypted with AES-256-GCM before storage; the key comes from `ENCRYPTION_KEY` and is never persisted in the database.
4. Credentials never appear in logs, error messages, or API responses.
5. Connection health (`sync_state`, `last_synced_at`, last error) is readable per mailbox.

---

## Prerequisites

| Needed | For | Why |
|---|---|---|
| **`ENCRYPTION_KEY` generated and set** in all three environments | AC3 | A 32-byte key someone must generate and store as a Vercel environment variable. **Losing it makes every stored credential unrecoverable** — it is the one secret on this project with no reset path |

*Note the asymmetry with every other secret here: a Clerk or Resend key can be rotated from a dashboard. This one decrypts data at rest, so rotation means re-encrypting every mailbox row. Story 2.1 should decide whether that path exists now or is accepted as post-MVP.*

---

## Tasks

## ⚠ This story owns Epic 2's schema, not just `mailboxes` (added 2026-08-07)

The whole-corpus pass asked, for each of the eighteen tables, which story creates it. **`conversations`, `messages`, `contacts` and `attachments` had no answer** — and `messages` is referenced by twenty-three stories, more than any other table in the product.

Nobody was careless. `migrations/0001_init.sql` creates all sixteen tables, correctly, and it is checked in — **so every story that opened it saw a complete schema.** But §6.8c rules `0001`–`0003` are never applied to Neon: the live schema comes from Drizzle. The only file creating these tables is the one file guaranteed not to run on the target database.

Story 2.6 states the assumption outright — *"the constraint already exists in the schema"* — which is true of the scratch box and true of nothing Neon will have.

**The failure is immediate, not eventual.** Story 2.4 inserts into `messages`. On a Drizzle-built Neon database it fails on its first statement, on day one of Epic 2, after Epic 1 reported green.

**Ruling (§6.7d): this story creates Epic 2's five tables**, the same way Story 4.1 owns Epic 4's — because it is the epic's first story and already creates `mailboxes`.

```
mailboxes  ·  conversations  ·  messages  ·  contacts  ·  attachments
```

**Bring the constraints, not only the columns** — §6.2's warning, with a second reason here: `0001` is where they are written, and it is the file nobody will run. Specifically:

| Constraint | On | Why it matters |
|---|---|---|
| `UNIQUE (tenant_id, provider_message_id)` | `messages` | **The idempotency guarantee for the whole ingest path.** Story 2.7's dedup and Story 2.4's `ON CONFLICT DO NOTHING` are both this one constraint |
| `UNIQUE (tenant_id, mailbox_id, thread_key)` | `conversations` | Story 2.6 assumes it exists |
| `CHECK (status IN ('open','pending','resolved','spam'))` | `conversations` | Restored to §6.2 on 2026-08-06; it was in `0001` only |
| `CHECK (direction IN ('inbound','outbound'))` | `messages` | |
| `scan_status DEFAULT 'not_scanned'` | `attachments` | §13.3 — **never `'pending'`**, which claims a queue that does not exist |

Story 5.1's classification columns land on `messages` later (§6.7a); they are nullable and this story does not need them.

### Task 1 — The tables (AC1, AC5) — **Epic 2's five, per §6.7d**

- [ ] **`mailboxes`, `conversations`, `messages`, `contacts`, `attachments`** in the Drizzle schema, with the constraints named above. RLS policy DDL goes in `packages/db/migrations/policies/` per the pattern Story 1.3 establishes — **a table Drizzle creates arrives with no policy**.
- [ ] `mailboxes` per the DDL below; the other four per §6.2 and `migrations/0001`, **reading `0001` for constraints §6.2 omits**.
- [ ] `sync_state`, `last_synced_at`, and a last-error field are readable per mailbox (AC5). These are what Story 2.2 AC5 and Story 1.7's `mailbox_broken` email read.
- [ ] Let Story 1.3's coverage test catch a missing policy rather than pre-empting it. That is the net working.

### Task 2 — `MailboxConnector` (AC2)

- [ ] The interface exactly as §4.1 specifies: `connect(tenantId, provider, code)`, `refresh(mailboxId)`, `fetchSince(mailboxId, cursor)`, `send(mailboxId, outboundId)`, `revoke(mailboxId)`.
- [ ] **Nothing outside `packages/email` may branch on provider.** That is the point of the interface, and Story 2.3 AC4 tests it with a shared conformance suite — write that suite here, with no implementations yet, so 2.2 and 2.3 are written against a test that already exists.
- [ ] **Define `RawMessage` here.** `fetchSince` returns them and §4.2 says both entry points normalise to one — but four stories referred to the type and none defined it (boundary audit SB-4). It must carry a **`provider_message_id` derived identically by every entry point**, because Story 2.7's dedup constraint is built on it: derive it differently in the webhook and the poller and the constraint never fires.
- [ ] The cursor is opaque `text`. Gmail uses a history id, Graph a delta token, IMAP a UID — **the caller must never interpret it**.

### Task 3 — Credential encryption (AC3, AC4)

- [ ] AES-256-GCM, key from `ENCRYPTION_KEY`, ciphertext into `credentials_encrypted bytea`. §13.1 and NFR11.
- [ ] **The key is never persisted in the database** (AC3) — it lives only in the environment, read through `server/env.ts` (§15.6).
- [ ] AC4 is a *negative* requirement and needs a negative test: credentials must not appear in logs, error messages, or API responses. **Assert it** — a test that triggers a connector failure and greps the captured log and the serialised error for the plaintext. A rule nobody has watched fail is indistinguishable from one that does not work.
- [ ] Redaction belongs in the error taxonomy (§15.9), not at each call site, or the one path that forgets is the one that leaks.

---

## Dev notes

### The table — [§6.2](../architecture/database-schema.md)

```sql
CREATE TABLE mailboxes (
  id                    uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id             uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  provider              text NOT NULL,     -- gmail | outlook | imap | inbound_webhook
  address               citext NOT NULL,
  display_name          text,
  credentials_encrypted bytea NOT NULL,
  sync_cursor           text,              -- opaque; never interpreted by the caller
  sync_state            text NOT NULL DEFAULT 'idle',
  last_synced_at        timestamptz,
  is_active             boolean NOT NULL DEFAULT true,
  connected_at          timestamptz NOT NULL DEFAULT now(),  -- §6.8e; moves on reconnect
  created_at            timestamptz NOT NULL DEFAULT now(),
  UNIQUE (tenant_id, address)
);

CREATE INDEX idx_mailboxes_tenant ON mailboxes (tenant_id) WHERE is_active;
```

The partial index matters: polling reads active mailboxes on every cron tick, and inactive ones accumulate.

> **✅ `connected_at` is its own column — ruled in [§6.8e](../architecture/database-schema.md), 2026-08-05.** Not `created_at`. `connect()` sets it on **every** successful connection including a reconnect; `created_at` is written once. They coincide until a mailbox is revoked and reconnected, at which point reusing `created_at` makes the backfill re-fetch a month of mail the product has already replied to.

### `MailboxConnector` — [§4.1](../architecture/components.md)

> Owns OAuth with Gmail / Microsoft Graph and IMAP credentials. Stores refresh tokens encrypted, refreshes on demand, exposes a uniform `fetchSince(cursor)` / `send(message)` interface **so the rest of the system never branches on provider**.

### Standards

- **§15.6** — `ENCRYPTION_KEY` is read in `server/env.ts` and nowhere else.
- **§15.9** — connector failures throw taxonomy classes. Redaction lives there.
- **§15.1** — repositories take a `tx` from `withTenant()`.

---

## Testing

| AC | Test |
|---|---|
| 2 | The conformance suite exists and is skipped with no implementations — 2.2 and 2.3 turn it on |
| 3 | Round-trip encrypt/decrypt; ciphertext differs across two encryptions of the same plaintext (GCM nonce) |
| 4 | **A forced connector failure leaves no plaintext credential in the log or the serialised error** |
| 5 | Health fields are readable and update on a sync |

---

## Change log

| Date | Version | Change | Author |
|---|---|---|---|
| 2026-08-05 | 0.1 | Drafted. Flagged `ENCRYPTION_KEY` as the one secret with no reset path |
| 2026-08-05 | 0.2 | `connected_at` ruled as its own column (§6.8e) | Architect |

---

## Dev Agent Record

*To be completed by the Dev agent.*

## QA Results

*Gate file: `docs/qa/gates/2.1-mailbox-model-and-connection-framework.yml`*

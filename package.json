{
  "name": "email-engine",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
  "check:shards": "node scripts/check-shards.mjs",
  "shard": "python scripts/reshard.py"
  },
  "devDependencies": {
    "turbo": "^2.10.8",
    "typescript": "5.9.3"
  }
}
